---
phase: 06-lock-exclude-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - gbgolf/web/templates/index.html
  - gbgolf/web/routes.py
  - gbgolf/web/static/style.css
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "After uploading CSVs, user sees a collapsible 'Lock / Exclude Players' section with a flat table of all eligible cards"
    - "Each card row has a Lock checkbox (card-level) and an Exclude checkbox; first card row per player also has a Lock Golfer checkbox"
    - "All checkboxes are inside the #reoptimize-form so they submit together with card_pool on Re-Optimize"
    - "POST /reoptimize parses lock_card, exclude_card, and lock_golfer form fields and writes them to session before building ConstraintSet"
    - "Checking Lock in the browser disables the Exclude checkbox for the same row (JS conflict prevention)"
    - "Checkboxes are pre-checked on re-render to reflect the constraint state returned from the route"
  artifacts:
    - path: "gbgolf/web/templates/index.html"
      provides: "Player pool <details> section inside #reoptimize-form; lock/exclude/lock-golfer checkboxes; JS conflict prevention"
      contains: "player-pool-section"
    - path: "gbgolf/web/routes.py"
      provides: "/reoptimize route parses checkbox fields, writes session, passes card_pool and locked_card_keys to template"
      contains: "request.form.getlist"
    - path: "gbgolf/web/static/style.css"
      provides: ".lock-golfer-empty CSS rule for muted empty Lock Golfer cells"
      contains: ".lock-golfer-empty"
  key_links:
    - from: "gbgolf/web/templates/index.html"
      to: "gbgolf/web/routes.py"
      via: "form action=/reoptimize, checkboxes name=lock_card/exclude_card/lock_golfer inside #reoptimize-form"
      pattern: "reoptimize-form"
    - from: "gbgolf/web/routes.py"
      to: "gbgolf/optimizer/constraints.py"
      via: "ConstraintSet(locked_cards=..., locked_golfers=..., excluded_cards=...) built from parsed form fields"
      pattern: "ConstraintSet"
    - from: "gbgolf/web/routes.py"
      to: "gbgolf/web/templates/index.html"
      via: "render_template passes card_pool, locked_card_keys, locked_golfer_set, excluded_card_keys"
      pattern: "locked_card_keys"
---

<objective>
Implement the player pool table UI (UI-01): add a collapsible `<details id="player-pool-section">` section inside `#reoptimize-form` showing all eligible cards with Lock, Lock Golfer, and Exclude checkboxes. Update the `/reoptimize` route to parse checkbox form fields and write session keys. Update both routes to pass `card_pool`, `locked_card_keys`, `locked_golfer_set`, and `excluded_card_keys` to the template.

Purpose: Make the 7 RED UI-01 tests from Plan 01 go GREEN.
Output: Updated index.html (player pool section + JS conflict prevention), updated routes.py (/reoptimize checkbox parsing + index route template kwargs), new .lock-golfer-empty CSS rule in style.css.
</objective>

<execution_context>
@C:/Users/silve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/silve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lock-exclude-ui/06-CONTEXT.md
@.planning/phases/06-lock-exclude-ui/06-RESEARCH.md
@.planning/phases/06-lock-exclude-ui/06-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From gbgolf/web/routes.py (current state — Phase 5):
```python
# index() POST — currently renders without card_pool / locked_card_keys kwargs:
return render_template(
    "index.html",
    validation=validation,
    result=result,
    show_results=True,
    lock_reset=lock_reset,
    card_pool_json=card_pool_json,
    # MISSING (Phase 6 adds): card_pool=..., locked_card_keys=set(), locked_golfer_set=set(), excluded_card_keys=set()
)

# reoptimize() POST — currently reads session only, does NOT parse checkboxes:
constraints = ConstraintSet(
    locked_cards=[tuple(k) for k in session.get("locked_cards", [])],
    locked_golfers=session.get("locked_golfers", []),
    excluded_cards=[tuple(k) for k in session.get("excluded_cards", [])],
    excluded_players=session.get("excluded_players", []),
)
# MISSING (Phase 6 adds): parse request.form.getlist("lock_card") etc., write to session
```

From gbgolf/web/templates/index.html (current state — Phase 5):
```html
<!-- Current form structure (simplified): -->
{% if show_results and card_pool_json %}
<input type="hidden" id="card-pool-data" name="card_pool" value="{{ card_pool_json | e }}" />
{% endif %}

{% if show_results and card_pool_json %}
<form method="post" action="{{ url_for('main.reoptimize') }}" id="reoptimize-form">
  <input type="hidden" name="card_pool" value="{{ card_pool_json | e }}" />
  <button type="submit">Re-Optimize</button>
</form>
{% endif %}
<!-- Phase 6: player pool <details> must go INSIDE the form, BEFORE the Re-Optimize button -->
```

From gbgolf/optimizer/constraints.py:
```python
# CardKey = tuple[str, int, float, str]  — (player, salary, multiplier, collection)
@dataclass
class ConstraintSet:
    locked_cards: list[CardKey] = field(default_factory=list)
    locked_golfers: list[str] = field(default_factory=list)
    excluded_cards: list[CardKey] = field(default_factory=list)
    excluded_players: list[str] = field(default_factory=list)
```

From gbgolf/data/models.py:
```python
@dataclass
class Card:
    player: str
    salary: int
    multiplier: float
    collection: str
    expires: date | None
    projected_score: float | None
    effective_value: float | None
    franchise: str
    rookie: str
```

Composite key encoding for checkbox value attributes:
    "{card.player}|{card.salary}|{card.multiplier}|{card.collection}"
    e.g. "Tiger Woods|11000|1.5|Core"

Decoding in route:
    parts = v.split("|")  # 4 parts
    key = (parts[0], int(parts[1]), float(parts[2]), parts[3])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /reoptimize route to parse checkboxes and write session</name>
  <files>gbgolf/web/routes.py</files>
  <action>
    Replace the reoptimize() function body with the Phase 6 version. Keep _serialize_cards and _deserialize_cards unchanged. Keep the index() route unchanged except for adding the missing template kwargs (see Task 2).

    New reoptimize() logic (in order):
    1. Read card_pool form field; error-return if missing or malformed (same as Phase 5).
    2. Define _parse_card_keys(raw_list) inline — splits pipe-delimited strings into (player, int(salary), float(mult), collection) tuples; skips malformed entries silently.
    3. Parse checkbox submissions:
         locked_cards  = _parse_card_keys(request.form.getlist("lock_card"))
         excluded_cards = _parse_card_keys(request.form.getlist("exclude_card"))
         locked_golfers = [v for v in request.form.getlist("lock_golfer") if v]
         excluded_players = session.get("excluded_players", [])   # preserved, not cleared
    4. Write parsed constraints to session:
         session["locked_cards"]    = [list(k) for k in locked_cards]
         session["locked_golfers"]  = locked_golfers
         session["excluded_cards"]  = [list(k) for k in excluded_cards]
         session["excluded_players"] = excluded_players
    5. Build ConstraintSet from parsed values (NOT from session re-read — use the already-parsed lists).
    6. Call pre-solve checks (check_conflicts, check_feasibility) BEFORE optimize(); render error template if either returns a non-None result.
    7. Call optimize(); render results.
    8. render_template call must include these new kwargs:
         card_pool=sorted(valid_cards, key=lambda c: (c.player, -c.salary)),
         locked_card_keys=set(locked_cards),
         locked_golfer_set=set(locked_golfers),
         excluded_card_keys=set(excluded_cards),

    CRITICAL — anti-patterns to avoid:
    - Do NOT use request.form.get("lock_card") — use getlist() for multi-value fields
    - Do NOT store Card objects in session — only the encoded key lists
    - Tuple conversion: session["locked_cards"] stores list-of-lists (JSON); ConstraintSet needs list-of-tuples; pass locked_cards (already tuples from _parse_card_keys) directly
    - session["locked_cards"] stores lists (JSON-safe): [list(k) for k in locked_cards]

    Note on pre-solve checks: the existing index() route builds ConstraintSet but never calls check_conflicts / check_feasibility before optimize(). The reoptimize() route should also call these. Import both from gbgolf.optimizer.constraints (already imported in routes.py). If check_conflicts(constraints) returns non-None, render_template with error=result_of_check_conflicts.message. Same for check_feasibility(valid_cards, constraints).
  </action>
  <verify>
    <automated>pytest tests/test_web.py::test_reoptimize_parses_lock_checkboxes tests/test_web.py::test_reoptimize_parses_exclude_checkboxes tests/test_web.py::test_reoptimize_parses_lock_golfer -x -q</automated>
  </verify>
  <done>
    3 route-parsing tests from Plan 01 pass GREEN.
    Existing reoptimize tests (test_reoptimize_returns_results, test_reoptimize_layout_identical, test_reoptimize_missing_card_pool, test_reoptimize_malformed_card_pool, test_reoptimize_uses_session_constraints) remain GREEN.
    session["locked_cards"] contains [["Player A", 11000, 1.5, "Core"]] after posting lock_card="Player A|11000|1.5|Core".
  </done>
</task>

<task type="auto">
  <name>Task 2: Add player pool section to index.html and pass template vars from index() route</name>
  <files>gbgolf/web/templates/index.html, gbgolf/web/routes.py, gbgolf/web/static/style.css</files>
  <action>
    ## index.html changes

    ### Restructure the reoptimize-form to wrap the player pool section

    Replace the current two-block structure:
    ```
    standalone hidden input   (id="card-pool-data")
    reoptimize-form           (just hidden input + button)
    ```

    With a single form that wraps the player pool `<details>`:
    ```html
    {% if show_results and card_pool_json %}
    <form method="post" action="{{ url_for('main.reoptimize') }}" id="reoptimize-form">
      <input type="hidden" name="card_pool" value="{{ card_pool_json | e }}" />

      <details id="player-pool-section">
        <summary>Lock / Exclude Players</summary>
        <table>
          <thead>
            <tr>
              <th>Lock</th>
              <th>Lock Golfer</th>
              <th>Exclude</th>
              <th>Player</th>
              <th>Collection</th>
              <th>Salary</th>
              <th>Multiplier</th>
              <th>Proj Score</th>
            </tr>
          </thead>
          <tbody>
            {% if card_pool %}
            {% set ns = namespace(prev_player="") %}
            {% for card in card_pool %}
            <tr>
              <td>
                <input type="checkbox"
                       name="lock_card"
                       value="{{ card.player }}|{{ card.salary }}|{{ card.multiplier }}|{{ card.collection }}"
                       class="lock-cb"
                       {% if locked_card_keys and (card.player, card.salary, card.multiplier, card.collection) in locked_card_keys %}checked{% endif %}
                />
              </td>
              <td>
                {% if card.player != ns.prev_player %}
                {% set ns.prev_player = card.player %}
                <input type="checkbox"
                       name="lock_golfer"
                       value="{{ card.player }}"
                       class="lock-golfer-cb"
                       {% if locked_golfer_set and card.player in locked_golfer_set %}checked{% endif %}
                />
                {% else %}
                <span class="lock-golfer-empty"></span>
                {% endif %}
              </td>
              <td>
                <input type="checkbox"
                       name="exclude_card"
                       value="{{ card.player }}|{{ card.salary }}|{{ card.multiplier }}|{{ card.collection }}"
                       class="exclude-cb"
                       {% if excluded_card_keys and (card.player, card.salary, card.multiplier, card.collection) in excluded_card_keys %}checked{% endif %}
                />
              </td>
              <td>{{ card.player }}</td>
              <td>{{ card.collection }}</td>
              <td>${{ card.salary }}</td>
              <td>{{ card.multiplier }}</td>
              <td>{{ "%.2f"|format(card.projected_score or 0.0) }}</td>
            </tr>
            {% endfor %}
            {% endif %}
          </tbody>
        </table>
      </details>

      <button type="submit">Re-Optimize</button>
    </form>
    {% endif %}
    ```

    Remove the old standalone `<input type="hidden" id="card-pool-data" ...>` block that precedes the form. The card_pool hidden field is now inside the form only.

    Page section order after change:
      Upload section (<details id="upload-section">)
      Error / exclusion report
      #reoptimize-form (containing: player-pool-section <details> + Re-Optimize button)
      Lineup results

    ### Add JS conflict prevention

    In the existing `<script>` block (below the upload-form and reoptimize-form listeners), append:
    ```javascript
    document.querySelectorAll(".lock-cb").forEach(function(lockCb) {
      lockCb.addEventListener("change", function() {
        var row = lockCb.closest("tr");
        var excludeCb = row.querySelector(".exclude-cb");
        if (excludeCb) excludeCb.disabled = lockCb.checked;
      });
    });
    document.querySelectorAll(".exclude-cb").forEach(function(excludeCb) {
      excludeCb.addEventListener("change", function() {
        var row = excludeCb.closest("tr");
        var lockCb = row.querySelector(".lock-cb");
        if (lockCb) lockCb.disabled = excludeCb.checked;
      });
    });
    ```

    Also initialize disabled state on page load for pre-checked checkboxes:
    ```javascript
    // Initialize disabled state for pre-checked checkboxes on page load
    document.querySelectorAll(".lock-cb:checked").forEach(function(lockCb) {
      var row = lockCb.closest("tr");
      var excludeCb = row && row.querySelector(".exclude-cb");
      if (excludeCb) excludeCb.disabled = true;
    });
    document.querySelectorAll(".exclude-cb:checked").forEach(function(excludeCb) {
      var row = excludeCb.closest("tr");
      var lockCb = row && row.querySelector(".lock-cb");
      if (lockCb) lockCb.disabled = true;
    });
    ```

    ## routes.py changes (index() POST only)

    Update the render_template call in the index() POST success path to pass the new kwargs:
    ```python
    return render_template(
        "index.html",
        validation=validation,
        result=result,
        show_results=True,
        lock_reset=lock_reset,
        card_pool_json=card_pool_json,
        card_pool=sorted(validation.valid_cards, key=lambda c: (c.player, -c.salary)),
        locked_card_keys=set(),        # session just cleared — no locks on fresh upload
        locked_golfer_set=set(),
        excluded_card_keys=set(),
    )
    ```

    ## style.css changes

    Append one new rule at the end of the file:
    ```css
    .lock-golfer-empty {
      display: block;
      min-width: 1rem;  /* preserves column width alignment */
      color: transparent;
    }
    ```

    CRITICAL — structural warning from research:
    The player pool <details> MUST be INSIDE the <form id="reoptimize-form">.
    If it is outside the form, checkboxes will not submit. Verify by checking that
    <details id="player-pool-section"> appears between <form ... id="reoptimize-form"> and </form>.
  </action>
  <verify>
    <automated>pytest tests/test_web.py::test_player_pool_section_rendered tests/test_web.py::test_player_pool_table_columns tests/test_web.py::test_lock_exclude_checkboxes_in_form tests/test_web.py::test_lock_golfer_first_row_only -x -q</automated>
  </verify>
  <done>
    All 4 player pool structure tests from Plan 01 pass GREEN.
    All 7 UI-01 tests from Plan 01 pass (3 from Task 1 + 4 from this task).
    Full suite still passes: pytest tests/test_web.py -x -q.
    id="player-pool-section" exists inside id="reoptimize-form" in rendered HTML.
    30 lock_card checkboxes and 30 lock_golfer checkboxes appear in the HTML after CSV upload.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
  pytest tests/test_web.py -x -q

Expected: All 7 UI-01 tests from Plan 01 GREEN. All prior Phase 3-5 tests GREEN.
UI-03 tests (test_lineup_lock_column_header, test_locked_card_shows_lock_icon, test_nonlocked_card_blank_lock_column) remain RED — they are addressed in Plan 03.
</verification>

<success_criteria>
- /reoptimize route parses lock_card, exclude_card, lock_golfer form fields via getlist()
- Session keys locked_cards, locked_golfers, excluded_cards written from form submission (not prior session state)
- index() POST passes card_pool=sorted list, locked_card_keys=set(), locked_golfer_set=set(), excluded_card_keys=set()
- reoptimize() POST passes card_pool=sorted list, locked_card_keys, locked_golfer_set, excluded_card_keys from parsed submission
- Player pool <details> section rendered with 8-column table inside #reoptimize-form
- Lock Golfer checkbox appears once per unique player (30 checkboxes for 30 unique players in test data)
- JS conflict prevention: checking Lock disables Exclude; checking Exclude disables Lock
- All 7 UI-01 tests GREEN; UI-03 tests still RED (correctly deferred to Plan 03)
</success_criteria>

<output>
After completion, create .planning/phases/06-lock-exclude-ui/06-02-SUMMARY.md
</output>
