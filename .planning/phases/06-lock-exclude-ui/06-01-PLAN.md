---
phase: 06-lock-exclude-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_web.py
autonomous: true
requirements:
  - UI-01
  - UI-03

must_haves:
  truths:
    - "10 new test functions exist in tests/test_web.py targeting UI-01 and UI-03 behaviours"
    - "All 10 new tests run and FAIL (RED) before any implementation changes are made"
    - "Existing Phase 5 tests remain GREEN — no regressions introduced by test additions"
  artifacts:
    - path: "tests/test_web.py"
      provides: "Phase 6 test coverage for UI-01 (player pool table, checkboxes, route parsing) and UI-03 (lock column)"
      contains: "test_player_pool_section_rendered"
  key_links:
    - from: "tests/test_web.py"
      to: "gbgolf/web/routes.py"
      via: "Flask test client POST /reoptimize with lock_card form fields"
      pattern: "test_reoptimize_parses_lock"
---

<objective>
Add 10 failing test functions to tests/test_web.py that fully specify the Phase 6 UI behaviours before any implementation begins. Tests cover: player pool section rendering after upload, correct table columns, checkboxes present inside the re-optimize form, Lock Golfer first-row-only rendering, route parsing of lock_card / exclude_card / lock_golfer form fields, lineup lock column header, lock icon on locked card rows, and blank lock cell on non-locked rows.

Purpose: Establish a RED test suite so that Plan 02 and Plan 03 can drive implementation to GREEN with verifiable confidence.
Output: 10 new test functions appended to tests/test_web.py; all existing tests remain GREEN; new tests are RED.
</objective>

<execution_context>
@C:/Users/silve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/silve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lock-exclude-ui/06-CONTEXT.md
@.planning/phases/06-lock-exclude-ui/06-RESEARCH.md
@.planning/phases/06-lock-exclude-ui/06-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From tests/test_web.py (existing helpers and fixtures):
```python
# Fixture — returns Flask test client
@pytest.fixture()
def client(): ...  # yields test client for gbgolf.web.create_app()

# Helper — POST two CSVs to index route
def _post_csvs(client, roster_csv, projections_csv) -> Response: ...

# Helper — builds a valid card_pool JSON string for /reoptimize tests
def _build_card_pool_json() -> str: ...

# Sample CSV constants
SAMPLE_ROSTER_CSV: str   # 30 valid players
SAMPLE_PROJECTIONS_CSV: str

# _VALID_PLAYERS list — 30 tuples of (player_name, salary, multiplier)
# First entry: ("Player A", 11000, 1.5)
```

From gbgolf/web/routes.py (current /reoptimize route — Phase 5 state):
```python
@bp.route("/reoptimize", methods=["POST"])
def reoptimize():
    # Reads card_pool form field, deserializes, reads session for ConstraintSet
    # Does NOT yet parse lock_card / exclude_card / lock_golfer form fields
    # Does NOT yet pass card_pool or locked_card_keys to render_template
```

Session keys written by Phase 6 /reoptimize route (after Plan 02):
    locked_cards      — list of [player, salary, multiplier, collection] lists
    locked_golfers    — list of player name strings
    excluded_cards    — list of [player, salary, multiplier, collection] lists
    excluded_players  — list of player name strings (preserved, not cleared)

Checkbox form field encoding (pipe-delimited composite key):
    name="lock_card"     value="{player}|{salary}|{multiplier}|{collection}"
    name="lock_golfer"   value="{player}"
    name="exclude_card"  value="{player}|{salary}|{multiplier}|{collection}"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UI-01 test functions (player pool table and route parsing)</name>
  <files>tests/test_web.py</files>
  <behavior>
    - test_player_pool_section_rendered: POST CSVs → response HTML contains id="player-pool-section"
    - test_player_pool_table_columns: POST CSVs → HTML contains Lock, Lock Golfer, Exclude, Player, Collection, Salary, Multiplier, Proj Score column headers in the player pool table
    - test_lock_exclude_checkboxes_in_form: POST CSVs → HTML contains input[name="lock_card"] and input[name="exclude_card"] inside id="reoptimize-form"
    - test_lock_golfer_first_row_only: POST CSVs → count of input[name="lock_golfer"] equals count of unique player names (not count of total cards); assert count < total card rows
    - test_reoptimize_parses_lock_checkboxes: POST /reoptimize with lock_card="Player A|11000|1.5|Core" → session["locked_cards"] contains ["Player A", 11000, 1.5, "Core"]
    - test_reoptimize_parses_exclude_checkboxes: POST /reoptimize with exclude_card="Player B|10500|1.4|Core" → session["excluded_cards"] contains ["Player B", 10500, 1.4, "Core"]
    - test_reoptimize_parses_lock_golfer: POST /reoptimize with lock_golfer="Player C" → session["locked_golfers"] contains "Player C"

    All 7 tests MUST be RED before Plan 02 implementation. Verify with:
      pytest tests/test_web.py::test_player_pool_section_rendered -x  (should FAIL)
  </behavior>
  <action>
    Append 7 new test functions to tests/test_web.py under a new section comment:
    "# Phase 06: UI-01 — Player pool table and /reoptimize checkbox parsing"

    For test_player_pool_section_rendered through test_lock_golfer_first_row_only:
    - Use _post_csvs(client, SAMPLE_ROSTER_CSV, SAMPLE_PROJECTIONS_CSV) to get HTML
    - Assert presence of specific HTML fragments (id attributes, input name attributes, th text)

    For test_lock_golfer_first_row_only:
    - _VALID_PLAYERS has 30 entries all with unique player names (no player appears twice in the CSV)
    - After upload, the player pool table should have exactly 30 lock_golfer checkboxes (one per player)
    - Assert html.count('name="lock_golfer"') == 30 AND html.count('name="lock_card"') == 30

    For test_reoptimize_parses_lock_checkboxes / exclude / golfer:
    - POST to /reoptimize with card_pool=_build_card_pool_json() PLUS the checkbox field
    - Use client.session_transaction() AFTER the response to read session state
    - Assert the parsed key appears in session["locked_cards"] (as a list of 4 elements)

    Note: All tests are intentionally RED — do not change routes.py or index.html in this task.
  </action>
  <verify>
    <automated>pytest tests/test_web.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    7 new test functions exist in tests/test_web.py under the Phase 06 UI-01 section.
    Existing tests (Phase 3-5) remain GREEN.
    New tests FAIL with AssertionError or KeyError (not ImportError or SyntaxError).
    pytest output shows FAILED for the 7 new tests and PASSED for all prior tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add UI-03 test functions (lock column in lineup output)</name>
  <files>tests/test_web.py</files>
  <behavior>
    - test_lineup_lock_column_header: POST /reoptimize → HTML contains th "Lock" in a lineup table header
    - test_locked_card_shows_lock_icon: POST /reoptimize with lock_card="Player A|11000|1.5|Core" → HTML contains "🔒" in lineup rows
    - test_nonlocked_card_blank_lock_column: POST /reoptimize with no lock_card fields → HTML does NOT contain "🔒"

    All 3 tests MUST be RED before Plan 03 implementation.
  </behavior>
  <action>
    Append 3 new test functions to tests/test_web.py under a new section comment:
    "# Phase 06: UI-03 — Lock column in lineup output"

    For test_lineup_lock_column_header:
    - POST _build_card_pool_json() to /reoptimize (no lock fields)
    - Assert "<th>Lock</th>" in html

    For test_locked_card_shows_lock_icon:
    - POST _build_card_pool_json() to /reoptimize WITH lock_card="Player A|11000|1.5|Core"
    - Assert "🔒" in html (Player A is locked, must appear in at least one lineup)

    For test_nonlocked_card_blank_lock_column:
    - POST _build_card_pool_json() to /reoptimize (no lock_card fields)
    - Assert "🔒" NOT in html

    Note: These tests are intentionally RED — do not change routes.py or index.html in this task.
  </action>
  <verify>
    <automated>pytest tests/test_web.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    3 new test functions exist in tests/test_web.py under the Phase 06 UI-03 section.
    Total new test count in this plan: 10 (7 from Task 1 + 3 from Task 2).
    All prior tests GREEN; all 10 new tests FAIL with AssertionError (not import/syntax errors).
  </done>
</task>

</tasks>

<verification>
After both tasks:
  pytest tests/test_web.py -x -q

Expected: All existing tests PASS. All 10 new Phase 6 tests FAIL with AssertionError.
The output should show something like: "10 failed, N passed" where N >= 18 (prior passing tests).
</verification>

<success_criteria>
- 10 new test functions added to tests/test_web.py (7 UI-01, 3 UI-03)
- All prior tests continue to pass
- New tests fail with AssertionError (confirming they are meaningful RED tests, not broken tests)
- No new imports or fixtures needed — existing client fixture and helpers are sufficient
</success_criteria>

<output>
After completion, create .planning/phases/06-lock-exclude-ui/06-01-SUMMARY.md
</output>
