---
phase: 03-web-application-and-deployment
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: false
requirements:
  - DEPL-01

must_haves:
  truths:
    - "App is accessible at gameblazers.silverreyes.net/golf in a real browser"
    - "User can upload real GameBlazers roster and projections CSVs and receive lineup results"
    - "Lineups are displayed grouped by contest with correct columns"
  artifacts:
    - path: "deploy/gbgolf.service"
      provides: "Installed and running on VPS"
    - path: "deploy/gameblazers.silverreyes.net.nginx"
      provides: "Active in Nginx on VPS"
  key_links:
    - from: "gameblazers.silverreyes.net/golf"
      to: "gbgolf Gunicorn service"
      via: "Nginx reverse proxy"
      pattern: "curl http://gameblazers.silverreyes.net/golf/"
---

<objective>
User deploys the app to the VPS following DEPLOY.md and verifies it is live and functional.

Purpose: DEPL-01 requires the app to be accessible at silverreyes.net — only the user can perform this final step on their VPS.
Output: Live app at gameblazers.silverreyes.net/golf verified by the user.
</objective>

<execution_context>
@C:/Users/silve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/silve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-web-application-and-deployment/03-CONTEXT.md
@.planning/phases/03-web-application-and-deployment/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Deploy to VPS</name>
  <files>deploy/DEPLOY.md</files>
  <action>
    Follow deploy/DEPLOY.md step by step. All config files are in the deploy/ directory — replace placeholder paths and deploy_user before copying to the VPS.
  </action>
  <what-claude-prepared>
    - deploy/gbgolf.service — systemd service unit, ready to copy to /etc/systemd/system/
    - deploy/gameblazers.silverreyes.net.nginx — Nginx server block, ready to enable
    - deploy/DEPLOY.md — complete step-by-step instructions with all commands

    All placeholders in the config files need to be replaced with actual VPS paths before use.
  </what-claude-prepared>
  <instructions>
    Follow the steps in `deploy/DEPLOY.md`:

    1. Copy project to VPS (git clone or scp)
    2. Create venv and install: `pip install -e .`
    3. Edit gbgolf.service — replace `<deploy_user>` and `/path/to/GBGolfOptimizer`
    4. Install and start systemd service
    5. Edit and enable Nginx config
    6. Smoke test: `curl -s -o /dev/null -w "%{http_code}" http://gameblazers.silverreyes.net/golf/`
       — expect 200

    If you hit a port conflict with Open Claw: run `sudo nginx -T` and `ss -tlnp` on the VPS to identify what's in use before placing the Nginx config.
  </instructions>
  <verify>
    <automated>MISSING — manual step; user runs curl on VPS as smoke test</automated>
  </verify>
  <done>curl http://gameblazers.silverreyes.net/golf/ returns HTTP 200</done>
  <resume-signal>Type "deployed" when the curl smoke test returns 200, or describe any error you encountered</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify live app with real data</name>
  <files>deploy/DEPLOY.md</files>
  <action>
    Open the live app in a browser and perform an end-to-end test with real GameBlazers roster and projections CSVs.
  </action>
  <what-built>
    Flask web app deployed behind Nginx at gameblazers.silverreyes.net/golf with Gunicorn serving it via systemd.
  </what-built>
  <how-to-verify>
    1. Open http://gameblazers.silverreyes.net/golf/ in a browser — the upload form should appear
    2. Upload a real GameBlazers roster CSV and a real projections CSV
    3. Click "Generate Lineups"
    4. Verify:
       - A loading overlay appears while optimization runs
       - The upload form collapses to "Change files" after results load
       - "The Tips" section appears with 3 lineups (or infeasibility notices)
       - "The Intermediate Tee" section appears below with 2 lineups
       - Each lineup table has Player, Collection, Salary, Multiplier, Proj Score columns
       - Lineup totals appear in both the header above the table AND the footer row within the table
       - Unmatched player report shows between form and results (if any players have no projection)
    5. Try changing one file and re-running — verify the form accepts the new upload without requiring both files again
  </how-to-verify>
  <verify>
    <automated>MISSING — manual browser verification required (visual UI + interactive upload flow)</automated>
  </verify>
  <done>Real CSV upload produces correct lineup display with all required columns and contest grouping</done>
  <resume-signal>Type "approved" if everything works as described, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Manual: App accessible at gameblazers.silverreyes.net/golf, real CSV upload produces correct lineup display.
</verification>

<success_criteria>
- curl http://gameblazers.silverreyes.net/golf/ returns HTTP 200
- Real GameBlazers CSVs produce correct lineup results in the browser
- Contest sections display in correct order (The Tips then The Intermediate Tee)
- Lineup tables contain correct columns and totals
- DEPL-01 requirement satisfied: app is accessible on the Hostinger KVM 2 VPS
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-application-and-deployment/03-03-SUMMARY.md`
</output>
