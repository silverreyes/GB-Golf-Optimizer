# AGENTS.md — pointer

Read `./CLAUDE.md` in this repo for project-specific rules, then the AI Knowledge
Base for shared standards and memory:

**Vault:** `E:\KnowledgeBase\AIKB` (WSL `/mnt/e/KnowledgeBase/AIKB`) — start with
`meta/agent-bootstrap.md`, then `memory/profile.md`, then relevant `global/` notes
(genre folders: `global/lessons/`, `global/gotchas/`, `global/patterns/`).

**Canonical checkout:** WSL `/home/silver/projects/GB-Golf-Optimizer`; any other
copy is non-canonical. Per-repo canonicity is owned by the vault's
`projects/_index.md` table — read it there, don't restate it.
